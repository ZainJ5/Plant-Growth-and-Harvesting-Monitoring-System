PORT=5000
NODE_ENV=development

# Required: JWT and session secrets (use long random strings in production)
JWT_SECRET=your-jwt-secret-here
SESSION_SECRET=your-session-secret-here

# Required: Plant.id API key for plant health (get one at https://web.plant.id/)
PLANTS_ID_API_KEY=your-plant-id-api-key

# Firebase / Firestore
DB_API_KEY=
DB_AUTH_DOMAIN=
DB_PROJECT_ID=
DB_STORAGE_BUCKET=
DB_SENDER_ID=
DB_APP_ID=

# Optional
JWT_EXPIRES=1d
